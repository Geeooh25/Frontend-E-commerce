
        
        // product
        const products = [
            // Small Chops
            {
                id: 1,
                name: "Meat Pie",
                category: "small-chops",
                price: 1000,
                image: "./IMAGE/MEAT PIE.jpg",
                description: "Flaky pastry filled with seasoned minced meat.",
                hasAddOns: false
            },
            {
                id: 2,
                name: "Snowcap Doughnut",
                category: "small-chops",
                price: 1500,
                image: "./IMAGE/snowcap doughnut new.jpg",
                description: "Nigerian ring dough filled with powdered sweet milk.",
                hasAddOns: false
            },
            {
                id: 3,
                name: "Puff Puff ",
                category: "small-chops",
                price: 1000,
                image: "./IMAGE/FRESH PUFF PUFF.jpg",
                description: "Soft, fluffy Nigerian dough balls, lightly sweetened.",
                hasAddOns: false
            },
             {
                id: 4,
                name: "Glizing Doughnuts(3pcs)",
                category: "small-chops",
                price: 3500,
                image: "./IMAGE/glizing doughnuts.jpg",
                description: "Yummy glizing doughnut.",
                hasAddOns: false
            },
            {
                id: 5,
                name: "Potato Fries",
                category: "small-chops",
                price: 1000,
                image: "./IMAGE/POTATO CHIPS.JPG",
                description: "Crunchy potato fries.",
                hasAddOns: false
            },
            {
                id: 6,
                name: "Sausage Roll (4pcs)",
                category: "small-chops",
                price: 1200,
                image: "./IMAGE/SAUSAGE ROLL.jpg",
                description: "Flaky pastry wrapped around savory sausage.",
                hasAddOns: false
            },
            {
                id: 7,
                name: "Milky Doughnuts (2pcs)",
                category: "small-chops",
                price: 2000,
                image: "./IMAGE/milky doughnut.jpg",
                description: "Nigerian ring dough filled with powdered sweet milk.",
                hasAddOns: false
            },
            {
                id: 8,
                name: "Chin Chin (chocolate & plain)",
                category: "small-chops",
                price: 1000,
                image: "./IMAGE/CHINchin new.jpg",
                description: "Crunchy Nigerian snack with perfect sweetness.",
                hasAddOns: false
            },
            {
                id: 9,
                name: "Snowcap Puff Puff",
                category: "small-chops",
                price: 1000,
                image: "./IMAGE/SNOWCAP PUFF pUFF 2.jpg",
                description: "Fluffy round dough with powdered flavour.",
                hasAddOns: false
            },
            
            // Cakes
            {
                id: 10,
                name: "Red Velvet Cake",
                category: "cakes",
                price: 8000,
                image: "./IMAGE/RED VELVET.jpg",
                description: "Moist red velvet cake.",
                hasAddOns: false
            },
            {
                id: 11,
                name: "Rich Chocolate Cake",
                category: "cakes",
                price: 4000,
                image: "./IMAGE/CHOCOLATE CAKE.jpg",
                description: "Rich creamy chocolate cake.",
                hasAddOns: false
            },
            {
                id: 12,
                name: "Fruit Cake ",
                category: "cakes",
                price: 8000,
                image: "./IMAGE/FRUIT tray 2.jpg",
                description: "Traditional Nigerian fruit cake with mixed fruits.",
                hasAddOns: false
            },
            {
                id: 13,
                name: "Cupcakes (4pcs)",
                category: "cakes",
                price: 3000,
                image: "./IMAGE/CUP CAKES of 4.jpg",
                description: "Assorted flavored cupcakes with beautiful frosting.",
                hasAddOns: false
            },
            
            // Cakes (fixed from "cake" to "cakes")
            {
                id: 14,
                name: "Bento Cake",
                category: "cakes",
                price: 7000,
                image: "./IMAGE/BENTO CAKE 2.jpg",
                description: "Crunchy cookies with real coconut flakes.",
                hasAddOns: false
            },
            {
                id: 15,
                name: "Foil Cake",
                category: "cakes",
                price: 1000,
                image: "./IMAGE/MINI FOIL CAKE.jpg",
                description: "Traditional Nigerian groundnut cookies.",
                hasAddOns: false
            },
            
            // Cakes (fixed from "cake" to "cakes")
            {
                id: 16,
                name: "Cake Perfeit",
                category: "cakes",
                price: 3000,
                image: "./IMAGE/cake perfeit.jpg",
                description: "Buttery, flaky French croissants.",
                hasAddOns: false
            },
            {
                id: 17,
                name: "Chocolate Foil Cake",
                category: "cakes",
                price: 1000,
                image: "./IMAGE/CHOCOLATE FOIL CAKE.jpg",
                description: "Soft rolls with cinnamon sugar and cream cheese icing.",
                hasAddOns: false
            },
            {
                id: 18,
                name: "Birthday Cake (6INCH)",
                category: "cakes",
                price: 15000,
                image: "./IMAGE/BIRTHDAY CAKE 6.jpg",
                description: "icing frosting cake.",
                hasAddOns: false
            },
             {
                id: 19,
                name: "Birthday Cake(12INCH)",
                category: "cakes",
                price: 20000,
                image: "./IMAGE/BIRTHDAY CAKE 6INCH.jpg",
                description: "icing frosting cake.",
                hasAddOns: false
            },
             {
                id: 20,
                name: "Birthday Cake(8INCH)",
                category: "cakes",
                price: 25000,
                image: "./IMAGE/BIRTHDAY CAKE 8INCH.jpg",
                description: "icing frosting cake.",
                hasAddOns: false
            },
             {
                id: 21,
                name: "Birthday Cake (10INCH)",
                category: "cakes",
                price: 35000,
                image: "./IMAGE/BIRTHDAY CAKE 10 INCH.jpg",
                description: "icing frosting cake.",
                hasAddOns: false
            },
             {
                id: 22,
                name: "Birthday Cake(10INCH)",
                category: "cakes",
                price: 45000,
                image: "./IMAGE/BIRTHDAY CAKE 10 INCH 3.jpg",
                description: "icing frosting cake.",
                hasAddOns: false
            },
             {
                id: 23,
                name: "Birthday Cake(10INCH)",
                category: "cakes",
                price: 40000,
                image: "./IMAGE/BIRTHDAY CAKE 10 INCH 2.jpg",
                description: "icing frosting cake.",
                hasAddOns: false
            },
             {
                id: 24,
                name: "Birthday Cake (16INCH)",
                category: "cakes",
                price: 80000,
                image: "./IMAGE/TALL CAKE .jpg",
                description: "icing frosting cake.",
                hasAddOns: false
            },
             {
                id: 27,
                name: "Birthday Cake(16INCH)",
                category: "cakes",
                price: 80000,
                image: "./IMAGE/TALL CAKE 2.jpg",
                description: "icing frosting cake.",
                hasAddOns: false
            },
             {
                id: 28,
                name: "Birthday Cake(8INCH)",
                category: "cakes",
                price: 10000,
                image: "./IMAGE/BIRTHDAYCAKE.jpg",
                description: "icing frosting cake.",
                hasAddOns: false
            },
            
             {
                id: 29,
                name: "Curved Birthday Cake",
                category: "cakes",
                price: 40000,
                image: "./IMAGE/curved BIRTHDAY CAKE design.jpg",
                description: "icing frosting cake.",
                hasAddOns: false
            },
            {
                id: 30,
                name: "Banana Bread",
                category: "pastries",
                price: 2500,
                image: "./IMAGE/BANANA BREAD.jpg",
                description: "Moist and flavorful homemade banana bread.",
                hasAddOns: false
            },
            
            // Drinks
            {
                id: 31,
                name: "Zobo Drink (1L)",
                category: "drinks",
                price: 2000,
                image: "./IMAGE/ZOBO.jpg",
                description: "Refreshing hibiscus drink with pineapple and ginger.",
                hasAddOns: false
            },
            {
                id: 32,
                name: "Chapman (1L)",
                category: "drinks",
                price: 2000,
                image: "./IMAGE/CHAPMAN.jpg",
                description: "Nigerian classic cocktail (non-alcoholic).",
                hasAddOns: false
            },
            {
                id: 33,
                name: "Smoothies",
                category: "drinks",
                price: 3000,
                image: "./IMAGE/smoothies.jpg",
                description: "Freshly smooth.",
                hasAddOns: false
            },
            {
                id: 34,
                name: "Cookies(6pcs)",
                category: "cookies",
                price: 1500,
                image: "./IMAGE/cookies.jpg",
                description: "crunchy.",
                hasAddOns: false
            },
             
        ];

        let cart = [];
        let currentProduct = null;
        let currentCombo = null;
        let addOnsTotal = 0;
        let comboAddOnsTotal = 0;
        let quantity = 1;
        let comboQuantity = 1;

        // Initialize the page
        document.addEventListener('DOMContentLoaded', function() {
            displayProducts(products);
            displayAdvancedProducts(products);
            
            // Load cart from localStorage with proper initialization
            const savedCart = localStorage.getItem('beedahtCart');
            if (savedCart) {
                try {
                    cart = JSON.parse(savedCart);
                    // Ensure all cart items have required properties
                    cart = cart.map(item => ({
                        ...item,
                        isCombo: item.isCombo || false,
                        addOns: item.addOns || '',
                        addOnsTotal: item.addOnsTotal || 0
                    }));
                } catch (e) {
                    console.error('Error loading cart:', e);
                    cart = [];
                }
            } else {
                cart = [];
            }
            
            updateCartCount();
            
            // Mobile menu toggle
            document.querySelector('.mobile-menu-btn').addEventListener('click', function() {
                document.querySelector('nav').classList.toggle('active');
            });
            
            // Checkout form submission
            document.getElementById('checkout-form').addEventListener('submit', function(e) {
                e.preventDefault();
                processOrder();
            });
            
            // Newsletter form submission
            document.querySelector('.newsletter-form button').addEventListener('click', function(e) {
                e.preventDefault();
                const emailInput = document.querySelector('.newsletter-form input');
                if (emailInput.value) {
                    showNotification('Thank you for subscribing to our newsletter!', 'success');
                    emailInput.value = '';
                } else {
                    showNotification('Please enter your email address', 'error');
                }
            });
            
            // Smooth scrolling for navigation links
            document.querySelectorAll('nav a').forEach(anchor => {
                anchor.addEventListener('click', function(e) {
                    e.preventDefault();
                    const targetId = this.getAttribute('href');
                    const targetSection = document.querySelector(targetId);
                    
                    if (targetSection) {
                        window.scrollTo({
                            top: targetSection.offsetTop - 80,
                            behavior: 'smooth'
                        });
                        
                        // Close mobile menu if open
                        document.querySelector('nav').classList.remove('active');
                    }
                });
            });
        });

        // Display products in the gallery
        function displayProducts(productsToShow) {
            const container = document.getElementById('products-container');
            container.innerHTML = '';
            
            // Show only 8 featured products
            const featuredProducts = productsToShow.slice(0, 8);
            
            featuredProducts.forEach(product => {
                const productCard = document.createElement('div');
                productCard.className = 'product-card';
                productCard.innerHTML = `
                    <img src="${product.image}" alt="${product.name}" class="product-img">
                    <div class="product-info">
                        <span class="product-category">${product.category}</span>
                        <h3>${product.name}</h3>
                        <p>${product.description}</p>
                        <div class="product-price">
                            <span class="price">₦${product.price.toLocaleString()}</span>
                            <button class="btn" onclick="openProductModal(${product.id})">Add to Cart</button>
                        </div>
                    </div>
                `;
                container.appendChild(productCard);
            });
        }

        // Display products in advanced shopping
        function displayAdvancedProducts(productsToShow) {
            const container = document.getElementById('advanced-products-container');
            container.innerHTML = '';
            
            productsToShow.forEach(product => {
                const productCard = document.createElement('div');
                productCard.className = 'product-card';
                productCard.innerHTML = `
                    <img src="${product.image}" alt="${product.name}" class="product-img">
                    <div class="product-info">
                        <span class="product-category">${product.category}</span>
                        <h3>${product.name}</h3>
                        <p>${product.description}</p>
                        <div class="product-price">
                            <span class="price">₦${product.price.toLocaleString()}</span>
                            <button class="btn" onclick="openProductModal(${product.id})">Add to Cart</button>
                        </div>
                    </div>
                `;
                container.appendChild(productCard);
            });
        }

        // Filter products by category
        function filterProducts(category) {
            // Update active filter button
            document.querySelectorAll('.filter-option').forEach(btn => {
                btn.classList.remove('active');
            });
            event.target.classList.add('active');
            
            if (category === 'all') {
                displayAdvancedProducts(products);
            } else {
                const filteredProducts = products.filter(product => product.category === category);
                displayAdvancedProducts(filteredProducts);
            }
            
            // Scroll to advanced shopping section
            document.getElementById('advanced-shop').scrollIntoView({ behavior: 'smooth' });
            
            // Close mobile menu if open
            document.querySelector('nav').classList.remove('active');
        }

        // Filter category from footer links
        function filterCategory(category) {
            event.preventDefault();
            filterProducts(category);
        }

        // Filter products by price
        function filterByPrice(priceRange) {
            let filteredProducts = [];
            
            switch(priceRange) {
                case 'under-2000':
                    filteredProducts = products.filter(product => product.price < 2000);
                    break;
                case '2000-5000':
                    filteredProducts = products.filter(product => product.price >= 2000 && product.price <= 5000);
                    break;
                case 'over-5000':
                    filteredProducts = products.filter(product => product.price > 5000);
                    break;
            }
            
            displayAdvancedProducts(filteredProducts);
        }

        // Sort products
        function sortProducts() {
            const sortValue = document.getElementById('sort-products').value;
            let sortedProducts = [...products];
            
            switch(sortValue) {
                case 'price-low':
                    sortedProducts.sort((a, b) => a.price - b.price);
                    break;
                case 'price-high':
                    sortedProducts.sort((a, b) => b.price - a.price);
                    break;
                case 'name':
                    sortedProducts.sort((a, b) => a.name.localeCompare(b.name));
                    break;
            }
            
            displayAdvancedProducts(sortedProducts);
        }

        // Open product detail modal
        function openProductModal(productId) {
            const product = products.find(p => p.id === productId);
            if (!product) return;
            
            currentProduct = product;
            addOnsTotal = 0;
            quantity = 1;
            
            document.getElementById('modal-product-name').textContent = product.name;
            document.getElementById('modal-product-img').src = product.image;
            document.getElementById('modal-product-description').textContent = product.description;
            updateProductPriceDisplay();
            document.getElementById('quantity-display').textContent = quantity;
            
            // Show/hide add-ons section based on product type
            const addOnsSection = document.getElementById('add-ons-section');
            if (product.hasAddOns) {
                addOnsSection.classList.remove('hidden');
                // Reset add-on checkboxes
                document.getElementById('extra-chicken').checked = false;
                document.getElementById('extra-sauce').checked = false;
                document.getElementById('extra-cheese').checked = false;
            } else {
                addOnsSection.classList.add('hidden');
            }
            
            document.getElementById('product-modal').style.display = 'block';
        }

        // Open combo detail modal with dynamic toppings
        function openComboModal(name, price, image) {
            currentCombo = { name, price, image };
            comboAddOnsTotal = 0;
            comboQuantity = 1;
            
            document.getElementById('combo-modal-name').textContent = name;
            document.getElementById('combo-modal-img').src = image;
            updateComboPriceDisplay();
            document.getElementById('combo-quantity-display').textContent = comboQuantity;
            
            // Get the add-ons container
            const addOnsContainer = document.getElementById('combo-add-ons');
            addOnsContainer.innerHTML = '';
            
            // Define different topping options based on combo type
            let toppings = [];
            
            if (name.includes('Jollof') || name.includes('Fried Rice')) {
                toppings = [
                    { id: 'extra-chicken', label: 'Extra Chicken', price: 1500 },
                    { id: 'extra-beef', label: 'Extra Beef', price: 500 },
                    { id: 'extra-fish', label: 'Extra Fish', price: 1500 },
                    { id: 'extra-plantain', label: 'Extra Plantain', price: 400 },
                    { id: 'extra-sauce', label: 'Extra Sauce', price: 300 }
                ];
            } else if (name.includes('Small Chops')) {
                toppings = [
                    { id: 'extra-meat kabab', label: 'Extra Meat Kabab', price: 1000 },
                    { id: 'extra-puff-puff', label: 'Extra Puff Puff ', price: 400 },
                    { id: 'extra-samosa', label: 'Extra Samosa (3pcs)', price: 500 },
                    { id: 'extra-springroll', label: 'Extra Spring Roll (3pcs)', price: 500 }
                ];
            } else if (name.includes('Doughnut')) {
                toppings = [
                    { id: 'extra-doughnut', label: 'Extra Doughnut', price: 500 },
                    { id: 'extra-puff-puff', label: 'Extra Puff Puff ', price: 400 },
                   
                ];
             } else if (name.includes('Ewa Agoyin')) {
                toppings = [
                    { id: 'extra-meat', label: 'Extra Meat', price: 500 },
                    { id: 'extra-egg', label: 'Extra Egg', price: 300 },
                    { id: 'extra-plantain', label: 'Extra Plantain', price: 400 },
                ];
             }   else if (name.includes('Spaghetti')) {
                toppings = [
                    { id: 'extra-chicken', label: 'Extra Chicken', price: 1500 },
                    { id: 'extra-egg', label: 'Extra Egg', price: 300 },
                   { id: 'extra-fish', label: 'Extra Fish', price: 1500 },
                ];

             }else if (name.includes('Ponmo Garnished veg')) {
                toppings = [
                  
                ];
            } else {
                // Default toppings for other combos
                toppings = [
                    { id: 'extra-chicken', label: 'Extra Chicken', price: 1500 },
                    { id: 'extra-plantain', label: 'Extra Plantain', price: 400 },
                    { id: 'extra-sauce', label: 'Extra Sauce', price: 300 }
                ];
            }
            
            // Create add-on options dynamically
            toppings.forEach(topping => {
                const addOnOption = document.createElement('div');
                addOnOption.className = 'add-on-option';
                addOnOption.innerHTML = `
                    <div class="add-on-info">
                        <input type="checkbox" id="combo-${topping.id}" onchange="updateComboAddOnPrice()">
                        <label for="combo-${topping.id}">${topping.label} (+₦${topping.price.toLocaleString()})</label>
                    </div>
                    <span class="add-on-price">₦${topping.price.toLocaleString()}</span>
                `;
                addOnsContainer.appendChild(addOnOption);
            });
            
            document.getElementById('combo-modal').style.display = 'block';
        }

        // Close modal
        function closeModal() {
            document.getElementById('product-modal').style.display = 'none';
            quantity = 1;
            addOnsTotal = 0;
        }

        // Close combo modal
        function closeComboModal() {
            document.getElementById('combo-modal').style.display = 'none';
            comboQuantity = 1;
            comboAddOnsTotal = 0;
        }

        // Increase product quantity
        function increaseQuantity() {
            quantity++;
            document.getElementById('quantity-display').textContent = quantity;
            updateProductPriceDisplay();
        }

        // Decrease product quantity
        function decreaseQuantity() {
            if (quantity > 1) {
                quantity--;
                document.getElementById('quantity-display').textContent = quantity;
                updateProductPriceDisplay();
            }
        }

        // Update product price display
        function updateProductPriceDisplay() {
            if (currentProduct) {
                const basePrice = currentProduct.price;
                const totalPrice = (basePrice + addOnsTotal) * quantity;
                document.getElementById('modal-product-price').textContent = `₦${totalPrice.toLocaleString()}`;
            }
        }

        // Update add-on price calculation
        function updateAddOnPrice() {
            addOnsTotal = 0;
            
            if (document.getElementById('extra-chicken') && document.getElementById('extra-chicken').checked) {
                addOnsTotal += 800;
            }
            if (document.getElementById('extra-sauce') && document.getElementById('extra-sauce').checked) {
                addOnsTotal += 300;
            }
            if (document.getElementById('extra-cheese') && document.getElementById('extra-cheese').checked) {
                addOnsTotal += 500;
            }
            
            updateProductPriceDisplay();
        }

        // Increase combo quantity
        function increaseComboQuantity() {
            comboQuantity++;
            document.getElementById('combo-quantity-display').textContent = comboQuantity;
            updateComboPriceDisplay();
        }

        // Decrease combo quantity
        function decreaseComboQuantity() {
            if (comboQuantity > 1) {
                comboQuantity--;
                document.getElementById('combo-quantity-display').textContent = comboQuantity;
                updateComboPriceDisplay();
            }
        }

        // Update combo price display
        function updateComboPriceDisplay() {
            if (currentCombo) {
                const basePrice = currentCombo.price;
                const totalPrice = (basePrice + comboAddOnsTotal) * comboQuantity;
                document.getElementById('combo-modal-price').textContent = `₦${totalPrice.toLocaleString()}`;
            }
        }

        // Update combo add-on price calculation
        function updateComboAddOnPrice() {
            comboAddOnsTotal = 0;
            
            // Get all combo add-on checkboxes
            const checkboxes = document.querySelectorAll('#combo-add-ons input[type="checkbox"]');
            
            checkboxes.forEach(checkbox => {
                if (checkbox.checked) {
                    const priceElement = checkbox.closest('.add-on-option').querySelector('.add-on-price');
                    const priceText = priceElement.textContent.replace('₦', '').replace(/,/g, '');
                    const price = parseInt(priceText);
                    
                    if (!isNaN(price)) {
                        comboAddOnsTotal += price;
                    }
                }
            });
            
            updateComboPriceDisplay();
        }

        // Toggle cart modal
        function toggleCart() {
            const cartModal = document.getElementById('cart-modal');
            if (cartModal.style.display === 'block') {
                cartModal.style.display = 'none';
            } else {
                updateCartDisplay();
                cartModal.style.display = 'block';
            }
        }

        // Add item to cart
        function addToCart() {
            if (!currentProduct) {
                showNotification('Please select a product first!', 'error');
                return;
            }
            
            // Calculate final price with add-ons
            let unitPrice = currentProduct.price + addOnsTotal;
            
            // Get selected add-ons details
            let addOnsDetails = [];
            if (document.getElementById('extra-chicken') && document.getElementById('extra-chicken').checked) {
                addOnsDetails.push('Extra Chicken');
            }
            if (document.getElementById('extra-sauce') && document.getElementById('extra-sauce').checked) {
                addOnsDetails.push('Extra Sauce');
            }
            if (document.getElementById('extra-cheese') && document.getElementById('extra-cheese').checked) {
                addOnsDetails.push('Extra Cheese');
            }
            
            const cartItem = {
                id: Date.now(),
                productId: currentProduct.id,
                name: currentProduct.name,
                quantity: quantity,
                price: unitPrice,
                image: currentProduct.image,
                category: currentProduct.category,
                isCombo: false,
                addOns: addOnsDetails.length > 0 ? addOnsDetails.join(', ') : '',
                addOnsTotal: addOnsTotal
            };
            
            // Check if similar item already in cart
            const existingItemIndex = cart.findIndex(item => 
                item.productId === cartItem.productId && 
                item.addOns === cartItem.addOns
            );
            
            if (existingItemIndex !== -1) {
                // Update quantity if item exists
                cart[existingItemIndex].quantity += quantity;
                showNotification(`${currentProduct.name} quantity updated in cart!`, 'success');
            } else {
                // Add new item
                cart.push(cartItem);
                showNotification(`${currentProduct.name} added to cart!`, 'success');
            }
            
            updateCartCount();
            updateCartDisplay();
            saveCartToStorage();
            closeModal();
        }

        // Add combo to cart
        function addComboToCart() {
            if (!currentCombo) {
                showNotification('Please select a combo first!', 'error');
                return;
            }
            
            // Calculate final price with add-ons
            let unitPrice = currentCombo.price + comboAddOnsTotal;
            
            // Get selected add-ons details
            let addOnsDetails = [];
            const checkboxes = document.querySelectorAll('#combo-add-ons input[type="checkbox"]:checked');
            checkboxes.forEach(checkbox => {
                const label = checkbox.closest('.add-on-info').querySelector('label').textContent;
                addOnsDetails.push(label);
            });
            
            const cartItem = {
                id: Date.now(),
                productId: 'combo-' + currentCombo.name.replace(/\s+/g, '-').toLowerCase(),
                name: currentCombo.name,
                quantity: comboQuantity,
                price: unitPrice,
                image: currentCombo.image,
                isCombo: true,
                addOns: addOnsDetails.length > 0 ? addOnsDetails.join(', ') : '',
                addOnsTotal: comboAddOnsTotal
            };
            
            // Check if similar combo already exists in cart
            const existingItemIndex = cart.findIndex(item => 
                item.productId === cartItem.productId && 
                item.addOns === cartItem.addOns
            );
            
            if (existingItemIndex !== -1) {
                // Update quantity if item exists
                cart[existingItemIndex].quantity += comboQuantity;
                showNotification(`${currentCombo.name} quantity updated in cart!`, 'success');
            } else {
                // Add new item
                cart.push(cartItem);
                showNotification(`${currentCombo.name} added to cart!`, 'success');
            }
            
            updateCartCount();
            updateCartDisplay();
            saveCartToStorage();
            closeComboModal();
        }

        // Update cart count in header
        function updateCartCount() {
            const totalItems = cart.reduce((total, item) => total + item.quantity, 0);
            document.querySelector('.cart-count').textContent = totalItems;
        }

        // Update cart display in modal
        function updateCartDisplay() {
            const cartItemsContainer = document.getElementById('cart-items');
            const cartTotalElement = document.getElementById('cart-total');
            
            cartItemsContainer.innerHTML = '';
            let total = 0;
            
            if (cart.length === 0) {
                cartItemsContainer.innerHTML = `
                    <div class="cart-empty">
                        <i class="fas fa-shopping-bag" style="font-size: 3rem; color: var(--neutral-medium); margin-bottom: 20px;"></i>
                        <p style="color: var(--text-medium); margin-bottom: 20px;">Your cart is empty</p>
                        <a href="#products" class="btn" onclick="toggleCart()" style="text-decoration: none;">Start Shopping</a>
                    </div>
                `;
            } else {
                cart.forEach(item => {
                    const itemTotal = item.price * item.quantity;
                    total += itemTotal;
                    
                    const cartItemElement = document.createElement('div');
                    cartItemElement.className = 'cart-item';
                    cartItemElement.style.cssText = `
                        display: flex;
                        justify-content: space-between;
                        align-items: center;
                        padding: 15px 0;
                        border-bottom: 1px solid var(--neutral-medium);
                    `;
                    
                    cartItemElement.innerHTML = `
                        <div class="cart-item-info" style="flex: 1;">
                            <h4 style="margin: 0 0 5px 0; color: var(--text-dark);">${item.name}</h4>
                            <div class="cart-item-details" style="font-size: 0.9rem; color: var(--text-medium); margin-bottom: 5px;">
                                ${item.isCombo ? '<span style="background: var(--primary-light); color: var(--primary-dark); padding: 2px 8px; border-radius: 10px; font-size: 0.8rem;">COMBO</span> ' : ''}
                                ${item.addOns ? ' | ' + item.addOns : ''}
                            </div>
                            <div class="cart-item-price" style="font-weight: 600; color: var(--primary);">₦${itemTotal.toLocaleString()}</div>
                        </div>
                        <div class="cart-item-actions" style="display: flex; align-items: center; gap: 10px;">
                            <button class="quantity-btn" onclick="updateQuantity(${item.id}, -1)" style="width: 30px; height: 30px; border-radius: 50%; border: 1px solid var(--neutral-medium); background: none; cursor: pointer; display: flex; align-items: center; justify-content: center;">-</button>
                            <span style="min-width: 30px; text-align: center;">${item.quantity}</span>
                            <button class="quantity-btn" onclick="updateQuantity(${item.id}, 1)" style="width: 30px; height: 30px; border-radius: 50%; border: 1px solid var(--neutral-medium); background: none; cursor: pointer; display: flex; align-items: center; justify-content: center;">+</button>
                            <button class="remove-item" onclick="removeFromCart(${item.id})" style="background: none; border: none; color: var(--error); cursor: pointer; font-size: 1.2rem; margin-left: 10px;">
                                <i class="fas fa-trash"></i>
                            </button>
                        </div>
                    `;
                    cartItemsContainer.appendChild(cartItemElement);
                });
            }
            
            cartTotalElement.textContent = total.toLocaleString();
        }

        // Update item quantity in cart
        function updateQuantity(itemId, change) {
            const itemIndex = cart.findIndex(item => item.id === itemId);
            if (itemIndex === -1) return;
            
            const oldQuantity = cart[itemIndex].quantity;
            cart[itemIndex].quantity += change;
            
            if (cart[itemIndex].quantity <= 0) {
                const removedItem = cart.splice(itemIndex, 1)[0];
                showNotification(`${removedItem.name} removed from cart`);
            } else {
                showNotification(`${cart[itemIndex].name} quantity updated to ${cart[itemIndex].quantity}`);
            }
            
            updateCartCount();
            updateCartDisplay();
            saveCartToStorage();
        }

        // Remove item from cart
        function removeFromCart(itemId) {
            const itemIndex = cart.findIndex(item => item.id === itemId);
            if (itemIndex === -1) return;
            
            const removedItem = cart[itemIndex];
            cart.splice(itemIndex, 1);
            
            updateCartCount();
            updateCartDisplay();
            saveCartToStorage();
            
            showNotification(`${removedItem.name} removed from cart`, 'warning');
        }

        // Proceed to checkout
        function proceedToCheckout() {
            if (cart.length === 0) {
                showNotification('Your cart is empty!', 'error');
                return;
            }
            
            // Update checkout summary
            const checkoutSummary = document.getElementById('checkout-summary');
            const checkoutTotal = document.getElementById('checkout-total');
            
            checkoutSummary.innerHTML = '';
            let total = 0;
            
            cart.forEach(item => {
                const itemTotal = item.price * item.quantity;
                total += itemTotal;
                
                const itemElement = document.createElement('div');
                itemElement.className = 'cart-item';
                itemElement.style.cssText = `
                    display: flex;
                    justify-content: space-between;
                    align-items: center;
                    padding: 10px 0;
                    border-bottom: 1px solid var(--neutral-medium);
                `;
                itemElement.innerHTML = `
                    <div class="cart-item-info">
                        <h4 style="margin: 0 0 5px 0; font-size: 0.9rem;">${item.name}</h4>
                        <div class="cart-item-details" style="font-size: 0.8rem; color: var(--text-medium);">
                            ${item.isCombo ? '<span style="background: var(--primary-light); color: var(--primary-dark); padding: 2px 6px; border-radius: 8px; font-size: 0.7rem;">COMBO</span> ' : ''}
                            ${item.addOns ? '<br><small>' + item.addOns + '</small>' : ''}
                        </div>
                    </div>
                    <div class="cart-item-price" style="font-weight: 600; color: var(--primary); font-size: 0.9rem;">
                        ₦${itemTotal.toLocaleString()} (${item.quantity}x)
                    </div>
                `;
                checkoutSummary.appendChild(itemElement);
            });
            
            checkoutTotal.textContent = total.toLocaleString();
            
            // Show checkout modal
            document.getElementById('cart-modal').style.display = 'none';
            document.getElementById('checkout-modal').style.display = 'block';
        }

        // Close checkout modal
        function closeCheckout() {
            document.getElementById('checkout-modal').style.display = 'none';
        }

        // Select payment method
        function selectPayment(method) {
            // Remove selected class from all options
            document.querySelectorAll('.payment-option').forEach(option => {
                option.classList.remove('selected');
            });
            
            // Add selected class to clicked option
            event.currentTarget.classList.add('selected');
            
            // Check the corresponding radio button
            document.getElementById(method).checked = true;
        }

        // Process order
        function processOrder() {
            const name = document.getElementById('checkout-name').value;
            const email = document.getElementById('checkout-email').value;
            const phone = document.getElementById('checkout-phone').value;
            const address = document.getElementById('checkout-address').value;
            const paymentMethod = document.querySelector('input[name="payment"]:checked');
            
            if (!paymentMethod) {
                showNotification('Please select a payment method!', 'error');
                return;
            }
            
            // In a real application, you would send this data to your backend
            console.log('Order Details:', {
                customer: { name, email, phone, address },
                paymentMethod: paymentMethod.value,
                items: cart,
                total: cart.reduce((total, item) => total + (item.price * item.quantity), 0)
            });
            
            // Show success message
            showNotification('Order placed successfully! We will contact you shortly.', 'success');
            
            // Reset cart and form
            cart = [];
            updateCartCount();
            saveCartToStorage();
            document.getElementById('checkout-form').reset();
            closeCheckout();
        }

        // Show quick order modal
        function showOrderModal(type) {
            const modal = document.getElementById('quick-order-modal');
            const title = document.getElementById('quick-order-title');
            const message = document.getElementById('quick-order-message');
            
            if (type === 'pickup') {
                title.textContent = 'Store Pickup Order';
                message.textContent = 'Place your order for pickup at our bakery. We\'ll prepare it fresh for you.';
            } else if (type === 'delivery') {
                title.textContent = 'Home Delivery Order';
                message.textContent = 'Place your order for home delivery. We\'ll deliver it fresh to your doorstep.';
            }
            
            modal.style.display = 'block';
        }

        // Show subscription modal
        function showSubscriptionModal() {
            const modal = document.getElementById('quick-order-modal');
            const title = document.getElementById('quick-order-title');
            const message = document.getElementById('quick-order-message');
            
            title.textContent = 'Subscription Service';
            message.textContent = 'Sign up for our subscription service to get regular deliveries of your favorite treats.';
            
            modal.style.display = 'block';
        }

        // Close quick order modal
        function closeQuickOrder() {
            document.getElementById('quick-order-modal').style.display = 'none';
        }

        // Submit quick order
        function submitQuickOrder() {
            const name = document.getElementById('quick-order-name').value;
            const phone = document.getElementById('quick-order-phone').value;
            const details = document.getElementById('quick-order-details').value;
            
            if (!name || !phone) {
                showNotification('Please fill in your name and phone number!', 'error');
                return;
            }
            
            // In a real application, you would send this data to your backend
            console.log('Quick Order Details:', {
                customer: { name, phone },
                orderDetails: details
            });
            
            // Show success message
            showNotification('Order request submitted! We will contact you shortly to confirm your order.', 'success');
            
            // Reset form and close modal
            document.getElementById('quick-order-name').value = '';
            document.getElementById('quick-order-phone').value = '';
            document.getElementById('quick-order-details').value = '';
            closeQuickOrder();
        }

        // Save cart to localStorage
        function saveCartToStorage() {
            localStorage.setItem('beedahtCart', JSON.stringify(cart));
        }

        // Show notification
        function showNotification(message, type = 'success') {
            // Create notification element
            const notification = document.createElement('div');
            notification.className = `notification ${type}`;
            
            // Add icon based on type
            let icon = '✓';
            if (type === 'error') icon = '✗';
            if (type === 'warning') icon = '⚠';
            if (type === 'info') icon = 'ℹ';
            
            notification.innerHTML = `
                <span style="font-size: 1.2em;">${icon}</span>
                <span>${message}</span>
            `;
            
            // Remove any existing notifications
            document.querySelectorAll('.notification').forEach(el => el.remove());
            
            document.body.appendChild(notification);
            
            // Animate in
            setTimeout(() => {
                notification.classList.add('show');
            }, 10);
            
            // Animate out and remove
            setTimeout(() => {
                notification.classList.remove('show');
                setTimeout(() => {
                    if (notification.parentNode) {
                        document.body.removeChild(notification);
                    }
                }, 300);
            }, 3000);
        }

        // Close modals when clicking outside
        window.onclick = function(event) {
            const modals = document.querySelectorAll('.modal');
            modals.forEach(modal => {
                if (event.target === modal) {
                    modal.style.display = 'none';
                    
                    // Reset modal states
                    if (modal.id === 'product-modal') {
                        quantity = 1;
                        addOnsTotal = 0;
                    } else if (modal.id === 'combo-modal') {
                        comboQuantity = 1;
                        comboAddOnsTotal = 0;
                    }
                }
            });
        }
